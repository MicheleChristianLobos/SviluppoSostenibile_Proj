extends layout

block content
  main
    section#input-coord
      .container
        center
          h2 Inserisci Coordinate Geografiche
        form(action="/api" method="get")
          label(for="lat") Latitudine: 
          input(type="text" id="lat" name="lat" required placeholder="es. 45.4642")
          br
          label(for="lon") Longitudine: 
          input(type="text" id="lon" name="lon" required placeholder="es. 9.1900")
          br
          center
            button(type="submit" formtarget="_blank" id="checkQualitaAria") Controlla Qualità dell'Aria